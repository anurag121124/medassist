import './globals.css';
import type { Metadata } from 'next';
import { Inter } from 'next/font/google';
import { Toaster } from '@/components/ui/sonner';

const inter = Inter({ subsets: ['latin'] });

export const metadata: Metadata = {
  title: 'MedAssist - AI-Powered Medical Assistance Platform',
  description: 'Comprehensive medical assistance with AI-powered symptom checking, health roadmaps, diet planning, and provider locator.',
  keywords: 'medical, health, AI, symptom checker, diet planner, healthcare',
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        {children}
        <Toaster />
      </body>
    </html>
  );
}